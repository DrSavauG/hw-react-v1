import React from 'react';
import './style.css';
import Logo from "../Logo/Logo";

const Footer = () => {
    return (
        <div className={"footer"}>
            <Logo/>
        </div>
    );
};

export default Footer;