import React from 'react';
import Layout from "../Layout/Layout";

const HomePage = () => {
    return (
        <Layout >
            HomePage-test
            </Layout>
    );
};

export default HomePage;